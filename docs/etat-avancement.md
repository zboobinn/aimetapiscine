# État d'avancement — ArmaPool

Point d'étape régénéré le 2026-07-17 par relecture intégrale de `docs/decisions.md` (673 lignes, entrées D1–D18 dont D4 amendée + A1–A3), du code (`src/`, `supabase/migrations/`, `scripts/`, `tests/`) et de l'historique git (`git log` complet — 41 commits, du 2026-07-07 au 2026-07-17 — et `git show --stat` sur les commits récents). Aucune modification de code effectuée pour ce document. Nombre de tests vérifié en exécutant `pnpm vitest run` au moment de la rédaction : **200 tests, 28 fichiers, tous verts**.

La version précédente (2026-07-10) classait 28, 29, 30 en « planifiées, non commencées ». Ce n'est plus le cas : les trois sont massivement implémentées depuis (73 → 200 tests). Voir section 6 pour une divergence significative découverte entre les dates de `docs/decisions.md` et l'historique git réel, ainsi qu'une mise à jour de documentation manquante sur les assets photo.

## 1. Specs terminées (code présent, cohérent avec la spec)

| Spec | Sujet | État | Notes |
|---|---|---|---|
| 02 | Setup | ✅ | Repo, tooling, structure en place. |
| 03 | Modèle de données | ✅ | 10 migrations locales (`20260707000000` → `20260714210000`), confirmées sur disque. Nombre non poussé sur le projet cloud non vérifiable à distance dans ce chantier — voir « Dettes ouvertes » pour un écart de décompte relevé dans `decisions.md` lui-même. |
| 04 | Catalogue produits | ⚠️ terminé structurellement, **données factices** | `data/catalog.json` : toujours 8 produits de **test** (vérifié, `grep -c '"slug"'`). `image_url` protégé par contrainte `CHECK` blind-shipping ; `pro_price_ht` retiré des colonnes lisibles par `anon` (23a). |
| 05 | Design CSS | ✅ | Tokens Tailwind v4 (`@theme`), composants `ui/*`. Direction « Nuancier » (D1) **désormais appliquée** (28, ci-dessous), en alias des tokens 05, pas en remplacement. `/design-system` (05) et `/_design` (28) coexistent, tous deux neutralisés en prod. |
| 06 | Responsive | ✅ | Intégré au squelette de pages (07) et à la homepage refaite (30). |
| 07 | Pages | ✅ | Toutes les routes prévues créées, ISR sur catalogue. `/` entièrement reconstruite (30, ci-dessous) — n'est plus une coquille. `/guides` reste un placeholder (`guides-data.ts`, `content/guides/` toujours vide hors `.gitkeep`). |
| 08 | Calculateur | ⚠️ terminé en logique, **coefficients provisoires** | Logique pure inchangée et réutilisée telle quelle dans le calculateur inline PDP (29b①) et le teaser homepage (30 §03) — confirmé par plusieurs relectures de code citées dans `decisions.md` (« moteur inchangé »). Rendements accessoires toujours à refaire dès réception des fiches techniques APF. |
| 09 | Panier/checkout | ✅ | Store Zustand (`slug`/quantité seulement, jamais de prix ni de `sku`, 23a), résolution serveur, cross-sell, remise pack. Point d'ajout étendu par la PDP (29c②) sans changer le mécanisme (`addPackLines`/`addCatalogLine` réutilisés tels quels). |
| 10 | Paiement Stripe | ✅ | Checkout + webhook validés bout en bout (test mode) ; idempotence vérifiée. Rate limiting Postgres livré en 23b. |
| 11 | Commandes (BL + facture) | ✅ code, ⚠️ **risque non résolu** | Génération PDF, Storage privé, historique de statut. Risque D18 (BL physique) toujours non tranché. |
| 12 | Livraison | ⚠️ terminé structurellement, **tarifs provisoires** | Deux modes coexistent, Corse incluse avec surcoût provisoire. Vérifié (2026-07-22) : le port ne dépend structurellement pas des lignes du panier, donc pas de risque de divergence PDP↔panier sur ce point précis. |
| 13 | Promotions | ✅ | Remise pack -5 % ; **durcie le 2026-07-23** (`decisions.md`, « partie B ») : ne s'applique plus que si le kit recommandé est coché en entier (auparavant, un seul accessoire mineur déclenchait la remise sur toute la membrane — corrigé, décision métier). |
| 14 | Auth (B2C + B2B) | ✅ | Inscription/connexion/reset B2C, parcours pro complet. Prix pro désormais surfacé aussi sur la PDP (buy-box + checklist accessoires, 29b②/③ et 29c② partie A), pas seulement sur les grilles. Emails Auth toujours en anglais (SMTP custom non branché). |
| 15 | Conventions API | ✅ | `lib/api/{errors,response,validate}.ts`, toutes les Route Handlers sur `{error:{code,message}}`. `/api/pricing/product-price` étendu (rôle b2b, `accessorySlugs`) sans changer la convention. |
| 17 | Emails transactionnels | ✅ | Opérationnels via Resend, validés par campagne de test manuelle. Sans domaine de production (`onboarding@resend.dev`, provisoire). |
| 18 | SEO technique | ✅ | `robots.ts`/`sitemap.ts`, `SEO_ALLOW_INDEXING` (défaut `false`, vérifié dans `.env.example`), JSON-LD Product/Breadcrumb/Organization/Website/FAQPage (FAQPage ajouté en 30 §09). `Organization` déplacé de `page.tsx` vers `layout.tsx` (30, site-wide). Zéro `AggregateRating` (grep vérifié dans `src/lib/seo/`). |
| 23 | Sécurité | ✅ (23a + 23b) | `sku`/`pro_price_ht` jamais côté client, rate limiting Postgres, headers de sécurité, CSP Report-Only. `/api/pricing/product-price` durci deux fois de plus depuis (29b②, 29c② partie A) en gardant le même patron (clés disjointes par rôle, calcul pro jamais exécuté pour un b2c). |
| 24 | Tests | ✅ | **200 tests Vitest, 28 fichiers, tous verts** (`pnpm vitest run`, vérifié à la rédaction — 27 fichiers `src/**/*.test.ts` + `tests/blind-shipping.spec.ts`, ce dernier bien inclus par `vitest.config.ts` malgré son extension `.spec.ts`). E2E Playwright : `@playwright/test` est une dépendance présente et `pnpm test:e2e` existe dans `package.json`, mais **aucun `playwright.config.ts` ni suite E2E commitée** — seulement des scripts Playwright « jetables » non versionnés, utilisés ponctuellement pour des vérifications manuelles citées dans `decisions.md`. `pnpm test:e2e` échouerait donc tel quel aujourd'hui. Cohérent avec « E2E hors périmètre V1 » mais la nuance (script présent, suite absente) n'était pas explicite dans la version précédente de ce document. |
| 26 | Env config | ✅ | `lib/env/` inchangé dans sa structure. `NEXT_PUBLIC_REMAKE_GUARANTEE=off` par défaut (vérifié). `COMPANY_*`/`INSEE_SIRENE_API_KEY` toujours vides (vérifié `.env.example`) — voir « Bloquants externes ». |
| 27 | Blind shipping — front public | ✅ code et tests, ⚠️ **critère de done non entièrement clos** | Inchangé sur le fond depuis la version précédente. D17 (URL de suivi, sans objet) et D18 (BL physique) toujours ouverts. Denylist et détection à 3 passes réutilisées et re-testées à chaque nouvelle surface (PDP, homepage) sans régression relevée. |
| 28 | Design system « Nuancier » | ✅ | **Changement majeur depuis la version précédente** (était « zéro implémentation »). Fondations (28a : tokens, `/_design` sous flag, garde-fous `no-animation-libs`/`nuancier-contrast`) + 7 primitives (28b : `Swatch`, `SwatchGroup`, `PriceBlock`, `SpecTable`, `CollapsibleSection`, `StickyBuyBox`, `Bleed`), toutes présentes dans `src/components/nuancier/` (vérifié). Intégrées et réutilisées sans duplication dans 29 et 30. Piège récurrent documenté et vérifié dans le code : depuis un Client Component, ne jamais importer le barrel `@/components/nuancier` (réexporte `PriceBlock` → `server-only`) — désormais aussi consigné dans `CLAUDE.md`. |
| 29 | Page produit | ✅ code et tests, ⚠️ **critère de done du fichier spec non coché** | **Changement majeur** (était « non implémentée, bloquée sur assets photo »). Structure + prix (29a/29.0), calculateur inline (29b①), prix pro (29b②/③), drawer ATC mobile (29c①), checklist de chantier + remise pack + garantie de reprise (29c②), deux correctifs d'invariant panier (2026-07-22/23) tous vérifiés présents dans `src/features/pdp/`. `docs/29-page-produit.md` §Critère de done : **les 10 cases restent toutes non cochées** dans le fichier, y compris des critères fonctionnellement satisfaits et testés selon `decisions.md` (ex. « bouton ATC actif au chargement » — cette relecture n'a pas coché les cases, ce n'est pas de son ressort, mais le constat est signalé). Les 4 critères Lighthouse/INP/budget JS restent structurellement bloqués sur les vraies photos (A3, voir section 6). Dette non corrigée, vérifiée dans le code : le highlight « 10 ans de garantie **fabricant** » (`src/app/membrane-armee/[gamme]/[couleur]/page.tsx:71`) contredit toujours la règle CLAUDE.md (distributeur, jamais fabricant) déjà corrigée ailleurs sur le site (30, 2026-07-17). |
| 30 | Homepage | ✅ code et tests, ⚠️ **budget JS et LCP/CLS non fermés** | **Changement majeur** (était « non implémentée »). Hero (§01, avec l'amendement D4 — carrousel auto, voir la mise à jour de `CLAUDE.md`/`docs/30-homepage.md` faite plus tôt dans cette session), barre de réassurance (§02), teaser calculateur (§03, réutilise `recalculatePdpBuyBoxAmounts` de 29 sans fork), nuancier hover (§04), how-it-works/bento/pro-space/FAQ+JSON-LD/footer scindé (§05–10), `llms.txt`. Renommage de marque `Membranes Armées` → **ArmaPool** appliqué (`SITE_BRAND`, vérifié : aucune occurrence résiduelle de l'ancien nom dans `layout.tsx`/`header`/`footer`). Budget JS de `/` mesuré et chiffré par `decisions.md` : **~323 KB gzip contre 130 KB visés** — dépassement préexistant (bundle partagé), pas introduit par le teaser (delta mesuré : +1,3 KB). LCP/CLS non fermés, même blocage A3 que 29. |
| — | Renommage de marque | ✅ | `ArmaPool` / `armapool.fr` sur toutes les surfaces client (JSON-LD, `<title>`, header/footer/`llms.txt`). Le dossier local, le `project_id` Supabase et le repo restent nommés `aimetapiscine` (jamais vu du client, décision assumée). `package.json` porte toujours `"name": "nextapp"` — ni `aimetapiscine` ni `armapool` ; non traité par la décision de renommage (qui ne visait que les surfaces client), signalé ici à titre d'observation mineure. |

## 2. Specs non commencées

| Spec | Sujet | État |
|---|---|---|
| 19 | Performance | Non auditée — aucune mention de Lighthouse/Core Web Vitals/`web-vitals` trouvée dans `src/` (grep vérifié). |
| 20 | Contenu (/guides) | Toujours un placeholder ; `content/guides/` ne contient qu'un `.gitkeep`. |
| 21 | Analytics | Toujours aucune intégration Plausible/Umami dans `src/` (grep vérifié). Choix jamais tranché — voir « Bloquants externes ». |
| 22 | RGPD / consentement | Les 4 pages légales (`cgv`, `mentions-legales`, `confidentialite`, `livraison-retours`) font toujours 20-31 lignes chacune (vérifié `wc -l`) — stubs inchangés. Aucun gestionnaire de consentement dans `src/`. |
| 25 | Déploiement | Flux Git décidé, aucune trace de déploiement Vercel réel (`.vercel/` absent, vérifié). |
| 31 | Preuve sociale / finition | Doc écrite, **zéro implémentation** (grep vérifié : aucune trace d'avis/modération dans `src/app`). §06 de la homepage (30) laisse explicitement la place vide avec un commentaire JSX, sans avis fabriqué. Modération a priori (D9) toujours non négociable. |

Numérotation (D8) inchangée : specs 27–31 après 18–26 ; `docs/refonte-visuelle.md`/`docs/annexe-brief-photo.md` restent le dossier de recherche/brief, pas des specs.

## 3. Spec reportée

| Spec | Sujet | État |
|---|---|---|
| 16 | Intégrations (simulateur 3D APF) | ⏸ **reportée sine die**, inchangé. `/simulateur-3d` reste un stub, exclu du sitemap. |

## 4. Bloquants externes

- **Domaine de production** — prévu **septembre**. Bloque toujours allow-list Supabase (14), config Vercel (25), expéditeur email définitif (17), `SEO_ALLOW_INDEXING` (18), SMTP custom Resend (17).
- **Immatriculation de la société** — prévue **septembre**. `COMPANY_NAME`/`COMPANY_SIREN`/`COMPANY_VAT`/`COMPANY_ADDRESS` toujours vides dans `.env.example` (vérifié) — bloquant factures légales (11) et mentions légales (22).
- **Fiches techniques APF** — tarifs, poids, conditionnements réels. Bloque toujours le calibrage définitif du catalogue (04), du calculateur (08) et des frais de port (12).
- **Assets photo (A3)** — **partiellement nuancé depuis la version précédente**, sans être résolu. Les plans `macro`/`echelle`/`soudure` restent des SVG neutres gris. Le plan `bassin` (les 3 coloris du catalogue) a été remplacé par des photographies « temporaires générées » (JPEG haute résolution, scène de piscine à débordement au coucher de soleil, non neutres) — le code (`src/lib/media/pool-media.ts:69-74`) documente explicitement qu'il s'agit de visuels provisoires à remplacer au shooting réel, et `placeholder: true` reste posé. Chemin critique des specs 29/30 pour Lighthouse LCP/CLS/budget JS toujours non fermé. Voir section 6 : ce changement d'assets n'a jamais fait l'objet de sa propre entrée dans `decisions.md`.
- **Choix Plausible vs Umami** — toujours jamais tranché. Bloque la spec 21.
- **Garantie de reprise sur erreur de cote (A1)** — toujours non arbitrée avec APF ; `NEXT_PUBLIC_REMAKE_GUARANTEE=off` (vérifié), le front fonctionne dans les 3 cas (`getRemakeGuaranteeCopy()`, désormais branché sur la PDP ET la barre de réassurance homepage).
- **Version marque blanche du simulateur 3D (16)** — toujours à négocier avec APF.
- **Clé API INSEE (Sirene)** — `INSEE_SIRENE_API_KEY` toujours vide (vérifié) ; parcours pro fonctionne en activation 100 % manuelle.

## 5. Dettes ouvertes

- **Sortir le projet de OneDrive.** Toujours non traitée (le chemin local reste sous `Documents\Projets\aimetapiscine`, non vérifiable indépendamment que ce dossier soit ou non synchronisé OneDrive depuis cette session — dette reprise telle quelle de `decisions.md`, qui la garde elle-même non cochée).
- **D17 — URL de suivi transporteur, sans objet.** Inchangé.
- **D18 — Que met APF physiquement dans le carton ?** Inchangé, question toujours ouverte.
- **Migrations Supabase cloud non appliquées** — **écart de décompte relevé dans `decisions.md` lui-même**, reporté tel quel depuis la version précédente de ce document sans avoir été corrigé : la dette parle de « 8 des 10 migrations » non poussées mais énumère **9** noms de fichiers (`order_documents`, `order_processing_error`, `pro_insee`, `store_settings`, `email_notifications`, `products_column_privileges`, `products_no_supplier_ref`, `rate_limits`, `drop_redundant_order_index`). 10 migrations existent bien localement (vérifié par `Glob`, `init_schema` inclus) ; si seule `init_schema` est réellement sur le projet cloud, l'énoncé correct serait « 9 des 10 ». **État réel du projet cloud non vérifiable dans ce chantier** (pas d'accès Supabase authentifié) — signalé sans trancher, à corriger par le porteur de projet.
- **`scripts/resanitize-storage.ts` jamais exécuté** — toujours vrai, aucun bucket Storage public de photos produit n'existe (les nouveaux visuels `bassin` vivent dans `public/media/placeholders/`, pas dans Supabase Storage — situation inchangée pour ce script).
- **Highlight PDP « garantie fabricant »** (`src/app/membrane-armee/[gamme]/[couleur]/page.tsx:71`) — dette déjà notée le 2026-07-17 dans `decisions.md` lors du correctif de marque sur la homepage, toujours non corrigée sur la PDP (vérifié par grep). Contredit la règle CLAUDE.md « distributeur, jamais fabricant », déjà appliquée partout ailleurs.
- **Documentation des assets `bassin` non journalisée** — voir section 6. Pas une dette de code, une dette de traçabilité : le remplacement des SVG neutres par des photos provisoires générées sur le plan `bassin` n'a jamais eu sa propre entrée dans `docs/decisions.md`, contrairement à la règle « mets à jour docs/decisions.md à chaque choix technique » (CLAUDE.md).
- **(Close, hors chantier de cette relecture)** `CLAUDE.md` §Règles du chantier visuel et `docs/30-homepage.md` (règle hero + critère de done) ont été amendés plus tôt dans cette même session pour refléter D4 (hero défilant) — cohérents avec `decisions.md` au moment de la rédaction de ce document.

## 6. Vérification de `docs/decisions.md`

Relecture intégrale (673 lignes, entrées D1–D18 dont D4 amendée + A1–A3) comparée au code, aux migrations et à l'historique git complet (`git log`, 41 commits) :

- **Incohérence significative — dates d'entrées postérieures à aujourd'hui, mais code déjà commité avant.** Neuf entrées portent une date comprise entre **2026-07-18 et 2026-07-23** (« Spec 29, passe 2/3 » à « Correctif 29c② partie B », plus « Spec 30, passe 2/3 : teaser calculateur »), alors que la date du jour est le **2026-07-17** (confirmé par la commande système `date`). Ce n'est pas du travail décrit par anticipation : `git log` montre que le code correspondant à CHACUNE de ces entrées est déjà commité, avec des dates de commit **antérieures ou égales** à aujourd'hui — les dates du journal sont avancées de 1 à 7 jours par rapport à l'historique git réel :
  | Entrée `decisions.md` (date journal) | Commit git correspondant (date réelle) |
  |---|---|
  | 2026-07-18 — Spec 29 passe 2/3 (calculateur inline) | `fd4ad1e` spec 29b1, **2026-07-13** |
  | 2026-07-19 / 2026-07-20 — Spec 29 passe 2/3 ②/③ (prix pro) | `1629de8` spec 29b2&3, **2026-07-13** |
  | 2026-07-21 — Spec 29 passe 3/3 ① (drawer mobile) | `b69dbbb` spec 29c1, **2026-07-15** |
  | 2026-07-22 — Spec 29 passe 3/3 ② + 2 correctifs | `43f86d5` spec 29c-2a, **2026-07-15** |
  | 2026-07-23 — Correctif 29c② parties A et B | `84decda`/`a291f8c`, **2026-07-16** |
  | 2026-07-23 — Spec 30 passe 2/3 (teaser calculateur) | `377e79e` spec 20-2, **2026-07-17** |
  Le code lui-même a été vérifié présent et fonctionnel indépendamment de cette anomalie (voir sections 1 et les vérifications ci-dessous) — l'écart est documentaire, pas fonctionnel, mais il est réel et devrait être corrigé (dates à rectifier ou raison de l'avance à expliciter).
- **Mise à jour non journalisée — assets `bassin`.** L'entrée du 2026-07-13 (« Couture media seam ») décrit « 12 SVG neutres (gris, jamais turquoise) ». Le code actuel (`src/lib/media/pool-media.ts`) montre que les 3 fichiers du plan `bassin` sont désormais des JPEG (476 à 658 Ko, photographie de piscine à débordement, non neutre) introduits par le commit `1a30720` (« redesign homepage, phase A et B », 2026-07-17 11:46) — aucune entrée `decisions.md` ne documente ce changement, alors que les entrées suivantes du même jour (« Redesign, passe D ») s'appuient dessus sans le signaler (mention du « coucher de soleil (cas limite) » comme si l'asset avait toujours existé sous cette forme).
- Chaque module cité dans les entrées 27 à 31 de `decisions.md` a été vérifié PRÉSENT sur le disque (`src/features/pdp/*`, `src/features/home/*`, `src/components/nuancier/*`) — aucune entrée ne décrit du code qui n'existe pas.
- Les correctifs les plus récents cités (`accessorySlugs` sur `/api/pricing/product-price`, `totalLabel` figé à `"TTC"`, `isChecklistPackFormed(checkedSlugs, allAccessorySlugs)`) ont été vérifiés littéralement présents dans le code, malgré l'anomalie de dates ci-dessus.
- Chaque migration SQL présente dans `supabase/migrations/` a une décision correspondante ; le décompte « 8 des 10 non poussées » présente lui-même une incohérence arithmétique avec sa propre énumération (9 noms) — voir « Dettes ouvertes », non corrigé ici (hors périmètre : lecture, pas correction de `decisions.md`).
- Le point de vigilance déjà signalé dans la version précédente de ce document (D16, exclusion `:!src/lib/server/**` inopérante) n'a pas été revérifié dans ce chantier — aucune modification de `docs/decisions.md` ni du code touchant ce point depuis.
- Garde-fou D2 (zéro lib d'animation) revérifié : `package.json` ne contient toujours `gsap`/`motion`/`framer-motion`/`lenis`/`aos` nulle part (grep vérifié).
